import React from "react";
import ReactDOM from "react-dom";

import { PageDashboard } from "./pages/dashboard";

const rootElement = document.getElementById("root");
ReactDOM.render(<PageDashboard />, rootElement);
